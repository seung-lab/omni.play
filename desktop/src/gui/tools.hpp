#pragma once
#include "precomp.h"

namespace om {
namespace tool {

enum mode {
  SELECT,
  PAN,
  PAINT,
  ERASE,
  FILL,
  SPLIT,
  JOIN,
  CUT,
  LANDMARK,
  ANNOTATE,
  ADVANCED,
  SHATTER
};

}  // namespace tool
}  // namespace om
