#pragma once
#include "precomp.h"

namespace om {
namespace tool {

enum mode {
  SELECT,
  PAN,
  PAINT,
  ERASE,
  FILL,
  SPLIT,
  CUT,
  LANDMARK,
  ANNOTATE,
  KALINA,
  SHATTER
};

}  // namespace tool
}  // namespace om